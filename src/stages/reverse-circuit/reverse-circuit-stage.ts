import { PlaceholderStage } from "../shared/placeholder-stage";
import type { Stage } from "../shared/stage";

export function createReverseCircuitStage(): Stage {
  return new PlaceholderStage("reverse-circuit");
}
