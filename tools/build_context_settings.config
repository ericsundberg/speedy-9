[paths]
# Scan the complete repository. The builder excludes .git, node_modules,
# dist, binary font contents, and generated context snapshots.
scan_roots =
    ..

output_file = snapshots/codebase-context.txt

[options]
max_file_kb = 250
include_svg = false
include_lockfiles = false
tree_only = false
